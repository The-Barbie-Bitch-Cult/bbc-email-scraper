[project]
name = "email-domain-scraper"
version = "0.1.0"
description = "Crawl a website domain and export exposed email addresses to CSV."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
email-scraper = "email_scraper.cli:main"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
